// This file was automatically generated by genny.
// Any changes will be lost if this file is regenerated.
// see https://github.com/mauricelam/genny

package query

// bytesResultGroupHeap is a heap storing a list of values.
// The ordering of such items are determined by `lessThanFn`.
// The smallest item will be at the top of the heap.
type bytesResultGroupHeap struct {
	dv         []bytesResultGroup
	lessThanFn func(v1, v2 bytesResultGroup) bool
}

// newBytesResultGroupHeap creates a new values heap.
func newBytesResultGroupHeap(
	initCapacity int,
	lessThanFn func(v1, v2 bytesResultGroup) bool,
) *bytesResultGroupHeap {
	return &bytesResultGroupHeap{
		dv:         make([]bytesResultGroup, 0, initCapacity),
		lessThanFn: lessThanFn,
	}
}

// RawData returns the underlying backing array in no particular order.
func (h bytesResultGroupHeap) RawData() []bytesResultGroup { return h.dv }

// Min returns the "smallest" heap element according to the `lessThan` function.
func (h bytesResultGroupHeap) Min() bytesResultGroup { return h.dv[0] }

// Len returns the number of items in the heap.
func (h bytesResultGroupHeap) Len() int { return len(h.dv) }

// Cap returns the heap capacity before a reallocation is needed.
func (h bytesResultGroupHeap) Cap() int { return cap(h.dv) }

// Less returns true if item `i` is less than item `j`.
func (h bytesResultGroupHeap) Less(i, j int) bool {
	return h.lessThanFn(h.dv[i], h.dv[j])
}

// Swap swaps item `i` with item `j`.
func (h bytesResultGroupHeap) Swap(i, j int) { h.dv[i], h.dv[j] = h.dv[j], h.dv[i] }

// Reset resets the internal backing array.
func (h *bytesResultGroupHeap) Reset() { h.dv = h.dv[:0] }

// Push pushes a value onto the heap.
func (h *bytesResultGroupHeap) Push(value bytesResultGroup) {
	h.dv = append(h.dv, value)
	h.shiftUp(h.Len() - 1)
}

// Pop pops a value from the heap.
func (h *bytesResultGroupHeap) Pop() bytesResultGroup {
	var (
		n   = h.Len()
		val = h.dv[0]
	)

	h.dv[0], h.dv[n-1] = h.dv[n-1], h.dv[0]
	h.heapify(0, n-1)
	h.dv = h.dv[0 : n-1]
	return val
}

// SortInPlace sorts the heap in place and returns the sorted data, with the smallest element
// at the end of the returned array. This is done by repeated swapping the smallest element with
// the last element of the current heap and shrinking the heap size.
// NB: The heap becomes invalid after this is called.
func (h *bytesResultGroupHeap) SortInPlace() []bytesResultGroup {
	numElems := len(h.dv)
	for len(h.dv) > 0 {
		h.Pop()
	}
	res := h.dv[:numElems]
	h.dv = nil
	h.lessThanFn = nil
	return res
}

func (h bytesResultGroupHeap) shiftUp(i int) {
	for {
		parent := (i - 1) / 2
		if parent == i || !h.Less(i, parent) {
			break
		}
		h.dv[parent], h.dv[i] = h.dv[i], h.dv[parent]
		i = parent
	}
}

func (h bytesResultGroupHeap) heapify(i, n int) {
	for {
		left := i*2 + 1
		right := left + 1
		smallest := i
		if left < n && h.Less(left, smallest) {
			smallest = left
		}
		if right < n && h.Less(right, smallest) {
			smallest = right
		}
		if smallest == i {
			return
		}
		h.dv[i], h.dv[smallest] = h.dv[smallest], h.dv[i]
		i = smallest
	}
}

// topNBytes keeps track of the top n values in a value sequence for the
// order defined by the `lessThanFn`. In particular if `lessThanFn` defines
// an increasing order (returning true if `v1` < `v2`), the collection stores
// the top N largest values, and vice versa.
type topNBytes struct {
	n          int
	lessThanFn func(v1, v2 bytesResultGroup) bool
	h          *bytesResultGroupHeap
}

// newTopNBytes creates a new top n value collection.
func newTopNBytes(
	n int,
	lessThanFn func(v1, v2 bytesResultGroup) bool,
) *topNBytes {
	return &topNBytes{
		n:          n,
		lessThanFn: lessThanFn,
		h:          newBytesResultGroupHeap(n, lessThanFn),
	}
}

// bytesAddOptions provide the options for adding a value.
type bytesAddOptions struct {
	CopyOnAdd bool
	CopyFn    func(v bytesResultGroup) bytesResultGroup
	CopyToFn  func(src bytesResultGroup, target *bytesResultGroup)
}

// Len returns the number of items in the collection.
func (v topNBytes) Len() int { return v.h.Len() }

// Cap returns the collection capacity.
func (v topNBytes) Cap() int { return v.h.Cap() }

// RawData returns the underlying array backing the heap in no particular order.
func (v topNBytes) RawData() []bytesResultGroup { return v.h.RawData() }

// Top returns the "smallest" value according to the `lessThan` function.
func (v topNBytes) Top() bytesResultGroup { return v.h.Min() }

// Reset resets the internal array backing the heap.
func (v *topNBytes) Reset() { v.h.Reset() }

// Add adds a value to the collection.
func (v *topNBytes) Add(val bytesResultGroup, opts bytesAddOptions) {
	if v.h.Len() < v.n {
		if opts.CopyOnAdd {
			val = opts.CopyFn(val)
		}
		v.h.Push(val)
		return
	}
	if min := v.h.Min(); !v.lessThanFn(min, val) {
		return
	}
	popped := v.h.Pop()
	if !opts.CopyOnAdd {
		v.h.Push(val)
		return
	}
	// Reuse popped item from the heap.
	opts.CopyToFn(val, &popped)
	v.h.Push(popped)
}

// SortInPlace sorts the backing heap in place and returns the sorted data.
// NB: The value collection becomes invalid after this is called.
func (v *topNBytes) SortInPlace() []bytesResultGroup {
	res := v.h.SortInPlace()
	v.h = nil
	v.lessThanFn = nil
	return res
}
